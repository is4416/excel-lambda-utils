# PolygonArea 使用例

## 説明
2列（X, Y）の範囲から、多角形の面積を計算します。  
範囲は少なくとも3行以上必要です。

## 使用例
= PolygonArea(A1:B10)

- A列：X座標  
- B列：Y座標
- 閉じた多角形の場合、最終行は最初の行と同じ座標にする必要はありません（関数内で自動で閉じます）。
- 座標は、半時計周りで配置する必要があります。
- 時計回りで配置した場合、面積が減算されます（穴あきポリゴン）。
